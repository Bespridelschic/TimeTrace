<Page
    x:Class="TimeTrace.View.MainView.ContactsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimeTrace.View.MainView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

		<ListBox Margin="5" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
			<TextBlock Text="Список контактов" FontSize="30" Foreground="SteelBlue"/>
			<ListBoxItem ToolTipService.Placement="Right">
				<ToolTipService.ToolTip>
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="*"/>
							<RowDefinition Height="0.4*"/>
						</Grid.RowDefinitions>
						<Image Grid.Row="0" Source="/Assets/user-192.png" Height="250"/>
						<TextBox Text="Фамилия Имя Отчество" Grid.Row="1" Foreground="SteelBlue" VerticalAlignment="Top"
								 HorizontalAlignment="Center" Margin="0, 2, 0, 0" FontSize="20"/>
					</Grid>
				</ToolTipService.ToolTip>
				<StackPanel Orientation="Horizontal">
					<Image Source="/Assets/user-48.png" Width="40"/>
					<TextBlock Text="Фамилия Имя Отчество" Margin="15, 0, 0, 0" Foreground="SteelBlue" FontSize="25"/>
				</StackPanel>
			</ListBoxItem>
		</ListBox>
	</Grid>
</Page>

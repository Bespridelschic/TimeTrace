<Page
    x:Class="TimeTrace.View.SignInPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimeTrace"
	xmlns:Data="using:TimeTrace.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Page.Resources>
		<Style TargetType="Control" x:Key="CommonAligmentStyle">
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="HorizontalAlignment" Value="Center"/>
		</Style>

		<Style TargetType="TextBox" BasedOn="{StaticResource CommonAligmentStyle}">
			<Setter Property="Height" Value="60"/>
			<Setter Property="Width" Value="250"/>
			<Setter Property="PlaceholderText" Value="Введите ваш Email..."/>
			<Setter Property="ToolTipService.Placement" Value="Top"/>
			<Setter Property="ToolTipService.ToolTip" Value="Введите данные что были указаны при регистрации"/>
		</Style>

		<Style TargetType="PasswordBox" BasedOn="{StaticResource CommonAligmentStyle}">
			<Setter Property="Height" Value="60"/>
			<Setter Property="Width" Value="250"/>
			<Setter Property="PlaceholderText" Value="Введите ваш пароль..."/>
			<Setter Property="ToolTipService.Placement" Value="Top"/>
			<Setter Property="ToolTipService.ToolTip" Value="Введите данные что были указаны при регистрации"/>
		</Style>

		<Style TargetType="Button" BasedOn="{StaticResource CommonAligmentStyle}">
			<Setter Property="Height" Value="30"/>
			<Setter Property="Width" Value="100"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="HorizontalAlignment" Value="Center"/>
			<Setter Property="Background" Value="{ThemeResource ApplicationPageBackgroundThemeBrush}"/>
			<Setter Property="ToolTipService.Placement" Value="Top"/>
			<Setter Property="ToolTipService.ToolTip" Value="Войти в систему"/>
		</Style>

		<Style TargetType="CheckBox" BasedOn="{StaticResource CommonAligmentStyle}">
			<Setter Property="Height" Value="60"/>
			<Setter Property="Width" Value="250"/>
			<Setter Property="ToolTipService.Placement" Value="Top"/>
			<Setter Property="ToolTipService.ToolTip" Value="Автоматический вход в систему при каждом запуске"/>
		</Style>
	</Page.Resources>

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="0.3*"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="0.6*"/>
			<RowDefinition Height="0.6*"/>
			<RowDefinition Height="0.4*"/>
			<RowDefinition Height="0.5*"/>
			<RowDefinition Height="0.5*"/>
		</Grid.RowDefinitions>

		<Grid Grid.Row="1">
			<TextBlock Name="HeaderText" Text="Time Trace" FontSize="60" VerticalAlignment="Center" HorizontalAlignment="Center"/>
		</Grid>

		<Grid Grid.Row="2">
			<TextBox Name="EmailTextBox" Header="Почта:" IsSpellCheckEnabled="False"/>
		</Grid>

		<Grid Grid.Row="3">
			<PasswordBox Name="PasswordTextBox" Header="Пароль:"/>
		</Grid>

		<Grid Grid.Row="4">
			<CheckBox Name="SavePasswordChBox" Content="Сохранить пароль" IsChecked="True"/>
		</Grid>

		<Grid Grid.Row="5">
			<Button Name="SignInButton" Content="Войти" Click="SignInButton_Click"/>
		</Grid>

		<Grid Grid.Row="6">
			<HyperlinkButton VerticalAlignment="Bottom" HorizontalAlignment="Right" Content="Нет аккаунта? Зарегистируйтесь!" Margin="0, 0, 10, 5"
							 ToolTipService.Placement="Top" ToolTipService.ToolTip="Перейти на страницу регистрации" Click="SignUpButton_Click" />
		</Grid>
	</Grid>
</Page>
